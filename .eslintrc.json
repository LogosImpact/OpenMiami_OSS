{
  "$schema": "https://json.schemastore.org/eslintrc",
  "root": true,
  "rules": {
    "no-restricted-globals": [
      "error",
      {
        "name": "localStorage",
        "message": "Project policy: no localStorage. Use React state or the database. See CONTRIBUTING.md."
      },
      {
        "name": "sessionStorage",
        "message": "Project policy: no sessionStorage. Use React state or the database. See CONTRIBUTING.md."
      }
    ],
    "no-restricted-syntax": [
      "error",
      {
        "selector": "MemberExpression[object.name='window'][property.name='localStorage']",
        "message": "Project policy: no window.localStorage. Use React state or the database."
      },
      {
        "selector": "MemberExpression[object.name='window'][property.name='sessionStorage']",
        "message": "Project policy: no window.sessionStorage. Use React state or the database."
      }
    ]
  },
  "overrides": [
    {
      "files": ["scripts/**", "api/**"],
      "rules": {
        "no-restricted-globals": "off",
        "no-restricted-syntax": "off"
      }
    }
  ]
}
